export type DeliveryType = {
    streetAddress: string,
    deliveryMethod: string,
    dateDelivery: string,
    departmentNumber: string,
    surName: string,
    firstName: string,
    patronymic: string,
    address: string;
    time: string;
}